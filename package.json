{
  "name": "wiki-craft-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "tauri": "frontend/node_modules/.bin/tauri",
    "frontend:build": "npm --prefix frontend run build"
  }
}
