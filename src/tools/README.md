# Tools

v1 exposes only the tools needed for URL-source wiki maintenance:

- `web_fetch`: bounded HTTP(S) fetch with readable text extraction and response headers.
- `read_file`: bounded line-range file reading inside the workspace.
- `write_file`: scoped text writing inside the workspace.

